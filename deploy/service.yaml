apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp
  namespace: k8s-mcp
  labels:
    app: k8s-mcp
spec:
  type: ClusterIP
  selector:
    app: k8s-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
